node_modules/
.next/
out/
build/

# Local env files (with real API keys)
.env.local
.env*.local

# Database
*.db
*.db-journal
prisma/*.db

# User-uploaded assets (don't ship into git)
public/uploads/*
!public/uploads/.gitkeep

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# TypeScript incremental build cache
*.tsbuildinfo

# pnpm internal probes (atomic-write detection); harmless 0-byte files
# left behind when the installer is interrupted.
.__unlink_probe__
_tmp_*

# Logs
*.log
npm-debug.log*

# Capacitor / Android — build artifacts and per-machine config
android/.gradle/
android/build/
android/app/build/
android/captures/
android/.idea/
android/local.properties
android/app/release/
android/app/src/main/assets/capacitor.config.json
android/app/src/main/assets/public/
*.iml
*.keystore
*.jks
*.apk
*.aab
