# 《回声相册 Echo Album》产品开发文档 V1.0

## 0. 当前技术决策

本项目先采用：

```text
扣子前后端一体部署
+ 扣子自带数据库
+ 云端 AI API
+ 环境变量保存 API Key
+ 不做 embedding
+ 不做本地模型部署
+ 视觉大模型顺带完成图片理解和 OCR
+ 关键词检索 + 标签匹配 + LLM 重排
```

第一版目标不是做一个完整相册 App，而是先打通：

> **手机访问网页 → 上传/拍照 → 录音/输入文字 → AI 生成记忆卡 → 存入数据库 → 自然语言搜索找回。**

---

## 1. 产品定位

### 1.1 产品名称

**回声相册 Echo Album**

### 1.2 一句话定义

**回声相册是一款给照片加上“声音背面”的记忆搜索工具。用户拍照后，可以录一句话或写一句备注，系统自动把照片、语音、文字、时间地点整理成可搜索的记忆卡片。**

### 1.3 核心价值

传统相册只能存照片，回声相册存的是：

```text
照片看到的东西
+ 图片里的文字
+ 用户当时说的话
+ 用户当时写的备注
+ 拍摄时间
+ 拍摄地点
+ AI 生成的标题、摘要、标签
```

用户后续不需要记得照片在哪，只要记得一点线索，比如：

```text
“我上次说很好吃的甜品”
“那个客户 Wi-Fi 密码截图”
“上周拍的那张票据”
“那天在咖啡店我还拍了什么”
```

系统就能找回对应照片和当时的上下文。

---

## 2. MVP 功能范围

### 2.1 第一版必须实现

| 功能 | 说明 |
|---|---|
| 手机网页访问 | 用户用手机浏览器打开扣子部署页面 |
| 图片上传/拍照 | 支持上传图片，优先兼容手机拍照入口 |
| 语音录入 | 用户可以录一段 10～20 秒语音 |
| 文字备注 | 用户可以手动输入一句补充说明 |
| AI 图片理解 | 视觉大模型生成图片描述、OCR 文本、初步标签 |
| ASR 语音转文字 | 把用户语音转成文本 |
| 记忆卡生成 | LLM 生成标题、摘要、标签、搜索文本 |
| 数据库存储 | 保存图片地址、音频地址、文本信息、标签等 |
| 自然语言搜索 | 用户输入问题，系统找回相关记忆 |
| 结果展示 | 返回照片、标题、摘要、原声、匹配理由 |

### 2.2 第一版暂时不做

```text
不做 embedding
不做向量数据库
不做本地模型部署
不做自动读取全手机相册
不做长期后台整理
不做人脸关系识别
不做复杂时间线故事
不做多人社交
不做相册清理
```

---

## 3. 总体技术架构

```text
手机浏览器
  ↓
扣子部署的 Web 页面
  ↓
前端采集图片 / 语音 / 文字
  ↓
后端接口接收数据
  ↓
调用视觉大模型处理图片
  ↓
调用 ASR 处理语音
  ↓
调用 LLM 生成记忆卡
  ↓
写入扣子数据库
  ↓
用户搜索
  ↓
关键词召回 + 标签匹配 + 时间地点过滤
  ↓
LLM 重排并生成回答
  ↓
返回照片 + 原声 + 记忆卡
```

---

## 4. 页面设计

### 4.1 首页

功能：

```text
展示产品名称
展示一句话介绍
入口按钮：
- 创建记忆
- 搜索记忆
- 查看最近记忆
```

首页文案建议：

```text
回声相册
给每张照片加上声音的背面。

拍一张照片，说一句话。
以后你只要记得一点感觉，就能把它找回来。
```

---

### 4.2 创建记忆页

用户操作流程：

```text
上传/拍摄图片
↓
录音，可选
↓
输入文字备注，可选
↓
点击“生成记忆卡”
↓
等待 AI 处理
↓
展示生成结果
↓
用户确认保存
```

页面组件：

| 组件 | 说明 |
|---|---|
| 图片上传按钮 | 支持相册选择和拍照 |
| 图片预览 | 上传后立即显示 |
| 录音按钮 | 开始录音 / 停止录音 / 重录 |
| 文字备注框 | 用户可以输入一句话 |
| 时间记录 | 自动记录当前时间 |
| 地点输入 | 第一版可以先手动输入，后续再调用定位 |
| 生成按钮 | 调用后端 AI 流程 |
| 保存按钮 | 保存最终记忆卡 |

---

### 4.3 记忆卡详情页

展示内容：

```text
照片
标题
摘要
标签
拍摄时间
地点
用户原声播放
语音转录文本
OCR 文本
用户备注
AI 图片描述
```

示例：

```text
标题：酒味很浓的提拉米苏

摘要：
这是一张甜品照片。你当时说这个提拉米苏酒味很浓、入口即化，并表示下次还想再来。

标签：
提拉米苏 / 甜品 / 好吃 / 咖啡 / 推荐 / 回访

拍摄时间：
2026-05-16 20:35

你的原声：
“这个提拉米苏酒味很浓，入口即化，下次还想来。”
```

---

### 4.4 搜索页

用户输入：

```text
我上次吃到特别好吃的甜品是哪张？
```

系统返回：

```text
你找的应该是这张“酒味很浓的提拉米苏”。

匹配原因：
你的语音记录里提到“酒味很浓、入口即化、下次还想来”，AI 标签中也包含“甜品、好吃、推荐”。
```

结果卡片展示：

| 内容 | 说明 |
|---|---|
| 缩略图 | 对应照片 |
| 标题 | AI 生成标题 |
| 摘要 | 一句话记忆说明 |
| 匹配理由 | 为什么这张照片被找回 |
| 原声播放 | 播放当时录音 |
| 查看详情 | 进入记忆卡详情页 |

---

## 5. 数据库设计

### 5.1 memories 表

建议字段如下：

```text
id
image_url
audio_url
created_at
location_text

user_note
speech_text
ocr_text
vision_caption

title
summary
tags
entities
search_text

status
error_message
```

### 5.2 字段说明

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 记忆 ID |
| image_url | string | 图片地址 |
| audio_url | string | 音频地址，可为空 |
| created_at | string | 创建时间 |
| location_text | string | 地点，可为空 |
| user_note | string | 用户手动输入文字 |
| speech_text | text | 语音转文字结果 |
| ocr_text | text | 图片中文字 |
| vision_caption | text | 图片视觉描述 |
| title | string | AI 生成标题 |
| summary | text | AI 生成摘要 |
| tags | array/string | 标签列表 |
| entities | json/string | 实体信息，比如店名、金额、人物、地点 |
| search_text | text | 用于搜索的综合文本 |
| status | string | processing / completed / failed |
| error_message | text | 错误原因 |

---

## 6. AI 处理流程

### 6.1 创建记忆流程

```text
用户上传图片、音频、文字
↓
保存原始文件
↓
视觉大模型处理图片
↓
ASR 处理音频
↓
LLM 汇总所有信息
↓
生成记忆卡
↓
写入数据库
```

---

### 6.2 视觉大模型输出格式

输入：图片

要求视觉大模型输出：

```json
{
  "vision_caption": "图片中是一份提拉米苏甜品，旁边有一杯咖啡。",
  "ocr_text": "Tiramisu / Latte / Dessert",
  "objects": ["提拉米苏", "咖啡", "盘子"],
  "scene": "餐厅 / 咖啡店",
  "possible_tags": ["甜品", "咖啡", "美食", "餐厅"],
  "important_details": ["甜品看起来像提拉米苏", "旁边有咖啡"]
}
```

注意：  
OCR 先让视觉大模型顺带做。后面如果发现截图、票据、密码场景不准，再补独立 OCR。

---

### 6.3 ASR 输出格式

输入：音频

输出：

```json
{
  "speech_text": "这个提拉米苏酒味很浓，入口即化，下次还想来。"
}
```

如果用户没有录音，则：

```json
{
  "speech_text": ""
}
```

---

### 6.4 记忆卡生成 Prompt 设计

输入信息：

```text
图片描述：{vision_caption}
OCR文字：{ocr_text}
语音转录：{speech_text}
用户备注：{user_note}
拍摄时间：{created_at}
拍摄地点：{location_text}
```

让 LLM 输出：

```json
{
  "title": "酒味很浓的提拉米苏",
  "summary": "这是一张甜品照片。用户当时觉得提拉米苏酒味浓、入口即化，并表示下次还想再来。",
  "tags": ["提拉米苏", "甜品", "好吃", "咖啡", "推荐", "回访"],
  "entities": {
    "food": ["提拉米苏"],
    "place": ["餐厅", "咖啡店"],
    "emotion": ["喜欢", "满足"],
    "action_intent": ["下次还来"]
  },
  "search_text": "提拉米苏 甜品 好吃 酒味很浓 入口即化 下次还想来 咖啡 餐厅 推荐 回访 用户觉得惊艳 满足"
}
```

---

## 7. 搜索逻辑设计

你现在不做 embedding，所以搜索采用：

> **LLM 查询改写 + 关键词召回 + 标签匹配 + 时间地点过滤 + LLM 重排**

这套方案足够支撑第一版 Demo。

---

### 7.1 搜索总体流程

```text
用户输入自然语言问题
↓
LLM 分析问题，提取关键词、时间、地点、意图
↓
数据库召回候选记忆
↓
按规则打分
↓
取 Top 5～10 条候选
↓
交给 LLM 二次判断
↓
返回最匹配结果和解释理由
```

---

### 7.2 查询意图分类

用户问题大致分成四类。

| 类型 | 示例 | 主要依赖 |
|---|---|---|
| 精确信息查询 | “那个 Wi-Fi 密码截图在哪？” | OCR、user_note、tags |
| 模糊记忆查询 | “我上次说很好吃的甜品？” | speech_text、summary、tags |
| 时间地点查询 | “上周在咖啡店拍了什么？” | created_at、location_text |
| 上下文查询 | “那天除了这个还拍了什么？” | 时间、地点、相邻照片 |

第一版重点做前三类。

---

### 7.3 查询改写 Prompt

用户输入：

```text
我上次吃到特别好吃的甜品是哪张？
```

让 LLM 转成结构化查询：

```json
{
  "intent": "find_memory",
  "query_type": "fuzzy_memory",
  "keywords": ["甜品", "好吃", "推荐", "喜欢", "下次还来"],
  "possible_entities": ["提拉米苏", "蛋糕", "咖啡", "冰淇淋", "餐厅"],
  "time_hint": "最近一次",
  "location_hint": "",
  "must_have": ["甜品"],
  "should_have": ["好吃", "推荐", "喜欢", "下次还来"]
}
```

再用这些关键词去数据库里查。

---

### 7.4 召回字段优先级

搜索时查这些字段：

```text
title
summary
tags
search_text
speech_text
user_note
ocr_text
vision_caption
location_text
created_at
```

字段权重建议：

| 字段 | 权重 | 原因 |
|---|---:|---|
| title | 5 | 标题高度浓缩 |
| tags | 5 | 标签适合检索 |
| search_text | 4 | 专门为搜索生成 |
| speech_text | 4 | 用户真实记忆来源 |
| user_note | 4 | 用户主动输入，可信度高 |
| summary | 3 | AI 整理后的描述 |
| ocr_text | 3 | 截图、票据、密码场景重要 |
| vision_caption | 2 | 图片视觉信息 |
| location_text | 2 | 地点查询有用 |
| created_at | 1 | 时间过滤有用 |

---

### 7.5 简单打分规则

可以先用很朴素的规则：

```text
score = 
标题命中数 × 5
+ 标签命中数 × 5
+ search_text 命中数 × 4
+ speech_text 命中数 × 4
+ user_note 命中数 × 4
+ summary 命中数 × 3
+ OCR 命中数 × 3
+ 图片描述命中数 × 2
+ 地点命中数 × 2
+ 时间加权
```

时间加权：

```text
如果用户说“最近 / 上次 / 前几天”：
  越新的记录加分越高

如果用户说“上周 / 昨天 / 某天”：
  只筛对应时间范围
```

---

### 7.6 候选结果重排

数据库先找出 Top 5～10 条候选，然后交给 LLM 判断。

输入给 LLM：

```json
{
  "user_query": "我上次吃到特别好吃的甜品是哪张？",
  "candidates": [
    {
      "id": "mem_001",
      "title": "酒味很浓的提拉米苏",
      "summary": "用户觉得提拉米苏酒味浓、入口即化，下次还想再来。",
      "tags": ["提拉米苏", "甜品", "好吃", "推荐"],
      "speech_text": "这个提拉米苏酒味很浓，入口即化，下次还想来。",
      "created_at": "2026-05-16 20:35"
    }
  ]
}
```

让 LLM 输出：

```json
{
  "best_match_id": "mem_001",
  "confidence": 0.92,
  "answer": "你找的应该是这张提拉米苏照片。",
  "reason": "这条记忆中同时出现了“甜品”“好吃”“下次还想来”，并且语音里提到“酒味很浓、入口即化”，和你的问题高度匹配。"
}
```

---

### 7.7 搜索结果返回策略

#### 高置信度

如果 `confidence >= 0.75`：

```text
直接返回最匹配结果
```

文案：

```text
我找到了，应该是这张。
匹配原因：你当时说“酒味很浓，入口即化，下次还想来”。
```

#### 中置信度

如果 `0.45 <= confidence < 0.75`：

```text
返回 2～3 个候选结果
```

文案：

```text
我找到几张可能相关的照片，你看看是不是其中之一。
```

#### 低置信度

如果 `confidence < 0.45`：

```text
不强行回答
给出改搜建议
```

文案：

```text
我暂时没有找到特别匹配的记忆。你可以换一个关键词试试，比如“甜品”“咖啡”“截图”“上周”。
```

---

## 8. 界面交互与失败兜底设计

### 8.1 图片上传失败

可能原因：

```text
文件太大
网络中断
格式不支持
平台上传限制
```

兜底文案：

```text
图片上传失败了。可以换一张小一点的图片，或重新选择 jpg/png/webp 格式。
```

交互：

```text
保留用户已输入文字
提供“重新上传”按钮
提供“压缩后重试”提示
```

限制建议：

```text
图片大小：最大 2MB～5MB
格式：jpg / png / webp
```

---

### 8.2 录音失败

可能原因：

```text
浏览器没有麦克风权限
手机浏览器不兼容
用户拒绝授权
录音格式不支持
```

兜底文案：

```text
暂时无法录音。你可以直接用文字给这张照片留一句话。
```

交互：

```text
显示文字输入框
允许跳过录音
保留图片上传结果
```

---

### 8.3 摄像头调用失败

可能原因：

```text
浏览器权限问题
手机系统限制
HTTPS 权限问题
```

兜底文案：

```text
暂时无法打开相机。你可以从相册上传一张照片。
```

交互：

```text
隐藏相机模式
显示上传图片按钮
```

第一版必须保留：

```text
上传图片模式
```

这样现场演示不怕摄像头权限翻车。

---

### 8.4 视觉大模型处理失败

可能原因：

```text
模型接口超时
图片过大
API Key 错误
内容识别失败
```

兜底策略：

```text
先保存原图和用户备注
状态标记为 failed 或 partial
允许用户稍后重试 AI 生成
```

兜底文案：

```text
图片理解暂时失败，但照片已经保存。你可以稍后重新生成记忆卡。
```

---

### 8.5 ASR 语音转文字失败

兜底策略：

```text
保留原始音频
speech_text 留空
允许用户手动补充文字
```

兜底文案：

```text
语音转文字失败了，但原声已经保存。你可以手动补一句备注。
```

---

### 8.6 LLM 生成记忆卡失败

兜底策略：

```text
保存基础信息：
- 图片
- 音频
- 用户备注
- 时间
- 地点

生成一张简易记忆卡：
标题：未命名记忆
摘要：这是一条尚未完成 AI 整理的记忆。
标签：空
状态：partial
```

兜底文案：

```text
AI 生成记忆卡失败，但你的照片和备注已经保存。可以稍后重试。
```

---

### 8.7 搜索不到结果

兜底文案：

```text
我暂时没有找到特别匹配的记忆。你可以换个说法试试，比如：
- 直接说物品：甜品、咖啡、票据、截图
- 说时间：昨天、上周、最近一次
- 说地点：咖啡店、教室、商场
```

页面显示推荐搜索词：

```text
甜品
截图
票据
上周
咖啡
密码
餐厅
```

---

### 8.8 搜索结果不确定

文案：

```text
我找到几张可能相关的照片，不确定你要的是哪一张。
```

展示多个候选，并给每个候选加匹配原因：

```text
候选 1：酒味很浓的提拉米苏
原因：语音里提到“入口即化，下次还来”。

候选 2：咖啡店的蛋糕
原因：标签中包含“甜品、咖啡、好吃”。
```

---

## 9. API 设计

### 9.1 创建记忆

```http
POST /api/memories
```

输入：

```text
image_file
audio_file，可选
user_note，可选
location_text，可选
created_at
```

返回：

```json
{
  "memory_id": "mem_001",
  "status": "completed",
  "title": "酒味很浓的提拉米苏",
  "summary": "这是一张甜品照片。用户当时觉得提拉米苏酒味浓、入口即化，并表示下次还想再来。"
}
```

---

### 9.2 搜索记忆

```http
POST /api/search
```

输入：

```json
{
  "query": "我上次吃到特别好吃的甜品是哪张？"
}
```

返回：

```json
{
  "answer": "你找的应该是这张提拉米苏照片。",
  "confidence": 0.92,
  "results": [
    {
      "memory_id": "mem_001",
      "title": "酒味很浓的提拉米苏",
      "summary": "用户当时觉得提拉米苏酒味浓、入口即化，并表示下次还想再来。",
      "image_url": "...",
      "audio_url": "...",
      "reason": "语音中出现“酒味很浓、入口即化、下次还想来”，与问题高度匹配。"
    }
  ]
}
```

---

### 9.3 获取记忆详情

```http
GET /api/memories/{id}
```

返回：

```json
{
  "id": "mem_001",
  "image_url": "...",
  "audio_url": "...",
  "title": "酒味很浓的提拉米苏",
  "summary": "...",
  "tags": ["提拉米苏", "甜品", "好吃"],
  "ocr_text": "...",
  "vision_caption": "...",
  "speech_text": "...",
  "user_note": "...",
  "created_at": "2026-05-16 20:35",
  "location_text": "合肥某商场"
}
```

---

## 10. 环境变量设计

代码里留空，由你在扣子环境变量中填写。

```env
OPENAI_API_KEY=
VISION_MODEL=
ASR_MODEL=
LLM_MODEL=
MODEL_BASE_URL=
```

注意：

```text
不要把真实 API Key 写进 GitHub
不要使用 NEXT_PUBLIC_ 前缀保存私密 Key
不要在前端直接调用大模型 API
所有 AI 调用都放在服务端
```

---

## 11. 模型调用策略

### 11.1 第一版优先追求快

AI 链路建议压缩成三步：

```text
视觉大模型：图片描述 + OCR + 初始标签
ASR 模型：语音转文字
LLM：生成最终记忆卡
```

如果没有录音，就跳过 ASR。

如果用户没有写备注，也不影响生成。

---

### 11.2 视觉模型输出要求

必须输出结构化 JSON，不要只返回自然语言。

要求字段：

```text
vision_caption
ocr_text
objects
scene
possible_tags
important_details
```

---

### 11.3 记忆卡模型输出要求

必须输出结构化 JSON。

要求字段：

```text
title
summary
tags
entities
search_text
```

其中 `search_text` 是你不用 embedding 时最重要的搜索字段。

---

## 12. Demo 样例数据设计

建议你提前准备 5～8 条样例记忆，防止现场 AI 或上传失败。

### 样例 1：甜品

```text
照片：提拉米苏
语音：这个提拉米苏酒味很浓，入口即化，下次还想来。
标签：提拉米苏、甜品、好吃、推荐、回访
搜索问题：我上次吃到特别好吃的甜品是哪张？
```

### 样例 2：Wi-Fi 密码截图

```text
照片：Wi-Fi 密码截图
OCR：SSID: Customer_WiFi Password: A88888888
标签：Wi-Fi、密码、客户、截图
搜索问题：上次那个客户 Wi-Fi 密码截图在哪？
```

### 样例 3：票据

```text
照片：收据
OCR：金额 128 元
标签：收据、金额、消费、报销
搜索问题：那张 128 元的收据在哪里？
```

### 样例 4：咖啡店

```text
照片：咖啡和电脑
语音：这个地方挺适合下午写东西。
标签：咖啡、学习、写作、安静、下午
搜索问题：我之前说适合写东西的咖啡店是哪家？
```

### 样例 5：课堂截图

```text
照片：课堂 PPT
OCR：热导率、声子谱、分子动力学
标签：课程、科研、材料、声子谱
搜索问题：我之前拍的那个声子谱课件在哪？
```

---

## 13. 开发顺序

### 第一步：做页面骨架

```text
首页
创建记忆页
搜索页
记忆详情页
```

先不用 AI，先保证页面能跳转。

---

### 第二步：打通数据库

实现：

```text
创建一条记忆
读取记忆详情
列出最近记忆
```

先用假数据测试。

---

### 第三步：打通图片和音频上传

实现：

```text
上传图片
预览图片
上传音频
播放音频
保存 image_url / audio_url
```

---

### 第四步：接入视觉大模型

实现：

```text
上传图片
调用视觉大模型
返回 caption / OCR / tags
展示在页面上
```

---

### 第五步：接入 ASR

实现：

```text
上传音频
调用语音转文字
返回 speech_text
展示在页面上
```

---

### 第六步：生成记忆卡

实现：

```text
整合 vision_caption / ocr_text / speech_text / user_note / time / location
调用 LLM
生成 title / summary / tags / search_text
保存数据库
```

---

### 第七步：实现搜索

实现：

```text
用户输入 query
LLM 提取关键词
数据库召回候选
规则打分
LLM 重排
返回结果
```

---

### 第八步：加失败兜底和 Demo 样例

实现：

```text
上传失败提示
录音失败提示
AI 失败重试
搜索为空提示
样例数据入口
```

---

## 14. 最终 MVP 验收标准

项目完成后，至少要能完成这三个演示。

### 演示 A：味觉记忆

```text
上传提拉米苏照片
录音：“这个提拉米苏酒味很浓，入口即化，下次还想来”
生成记忆卡
搜索：“我上次吃到特别好吃的甜品是哪张？”
返回提拉米苏照片和原声
```

### 演示 B：截图检索

```text
上传 Wi-Fi 密码截图
视觉模型提取 OCR
搜索：“客户 Wi-Fi 密码截图在哪？”
返回对应截图
```

### 演示 C：模糊记忆

```text
上传咖啡店照片
录音：“这个地方挺适合下午写东西”
搜索：“我之前说适合写东西的地方是哪？”
返回咖啡店照片
```

---

## 15. 当前版本技术总述

当前版本的技术路线可以总结为：

> **回声相册 V1 不依赖 embedding，而是把每张照片转化为结构化记忆卡片。系统通过视觉大模型提取图片描述和 OCR，通过 ASR 提取用户原声文字，再用 LLM 生成标题、摘要、标签和搜索文本。后续搜索时，系统先用 LLM 把用户问题改写成关键词，再通过数据库字段匹配召回候选记忆，最后让 LLM 重排并解释匹配原因。**

这条路线的优点是：

```text
开发快
适合扣子部署
不需要向量数据库
不需要本地模型
Demo 稳定
方便解释给评委
```

第一版只要把下面这个闭环跑通，就可以开始进入可演示状态：

```text
手机打开网页
→ 上传/拍照
→ 录音/输入文字
→ AI 生成记忆卡
→ 存入数据库
→ 自然语言搜索
→ 找回照片和原声
```

这就是现在可以开始干活的版本。
