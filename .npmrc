# Faster registry for users in mainland China.
# Comment this out if you have direct fast access to registry.npmjs.org.
registry=https://registry.npmmirror.com/

# Hoisting pattern for Next.js compatibility (works around some shamefully-hoist issues)
shamefully-hoist=false
